[project]
name = "agent-reliability-lab"
version = "0.1.0"
description = "Deterministic reliability baseline for the AI Agent engineering series."
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
