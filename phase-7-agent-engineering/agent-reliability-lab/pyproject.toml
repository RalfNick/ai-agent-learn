[project]
name = "agent-reliability-lab"
version = "1.1.1"
description = "Deterministic contracts, evals, runtime boundaries, recovery, tracing, memory, graph, human-control, and production-loop reviews for the AI Agent engineering series."
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
