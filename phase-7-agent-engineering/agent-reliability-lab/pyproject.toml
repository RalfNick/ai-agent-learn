[project]
name = "agent-reliability-lab"
version = "1.0.0"
description = "Deterministic contracts, evals, runtime boundaries, recovery, tracing, memory, graph, and human-control reviews for the AI Agent engineering series."
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
