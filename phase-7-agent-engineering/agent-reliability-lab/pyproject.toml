[project]
name = "agent-reliability-lab"
version = "0.3.0"
description = "Deterministic contracts, evals, and context packets for the AI Agent engineering series."
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
