[project]
name = "agent-reliability-lab"
version = "0.6.0"
description = "Deterministic contracts, evals, Context Packets, Harness, Tool Engineering, and Durable Loop fault checks for the AI Agent engineering series."
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
