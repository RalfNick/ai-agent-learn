[project]
name = "agent-cli-lab"
version = "0.2.0"
description = "A deterministic lab for Agent CLI process contracts and CLI/MCP capability adapters."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
mcp = ["mcp==2.0.0"]

[project.scripts]
agent-lab = "agent_cli_lab.cli:main"

[tool.unittest]
start-directory = "tests"

